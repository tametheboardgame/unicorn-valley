---
id: R6.5-WP19C
title: Progressive unicorn creator
status: approved
autonomy: amber
depends_on: [R6.5-WP18K]
parallel_safe: false
human_gate: visual
---

# R6.5-WP19C - Progressive unicorn creator

Approved by David on 2026-09-08 as part of the remediation plan. Begin only when this package's dependencies and applicable human gates permit.

## Governing contract

Read `docs/2026-09-08-REMEDIATION-PROPOSAL.md`, especially Shared acceptance contract, Roadmap reconciliation and Delivery and resumption. The original feedback ledger and accepted WP18I/J shell remain protected. The section below is the bounded scope; the proposal is authoritative if duplicated wording differs.

## Scope and execution contract progressive unicorn creator

Autonomy: Amber visual design. Dependencies: WP18K. Finding: A01.

Design:

- David rejected the first implemented preview on 9 September 2026 because of messy alignment, text behind the unicorn, tiny Horn controls and redundant always-visible renaming. That preview is not an acceptable fallback.
- The approved implementation authority is `docs/design/wp19c-approved-creator-concept.jpg`: a cream two-panel inner interface with lavender/plum controls, gold selection/action states, generous spacing and a large fully contained unicorn preview. The tablet casing and promotional background are reference framing, not game chrome.
- Landscape: large preview on the left; the name and pencil-only rename entry above it; six clear categories on the right: Colours, Mane, Tail, Horn, Markings and Accessories. There is no Main/Name category and Mane and Tail remain separate.
- Selected categories use large direct style cards with an explicit border/check and relevant large colour swatches. Cards must depict supported IDs through the same appearance renderer as preview/world; concept-only proposed assets must never become misleading thumbnails.
- Show only the selected category's options, with a visible selection and child-readable labels. Colours exposes body/eyes; Mane & Tail groups style and colour without stacking every option on one screen.
- Portrait: preview above a scrollable category area, with an accessible primary action. Pencil-triggered name editing stays visible when the keyboard opens; footer never covers options.
- Back, Randomise and Save/Start have consistent meaning. Keep reset/undo behaviour clear; randomise in edit mode should not unexpectedly rename the existing unicorn.
- Show the same appearance in preview and world. Fit the longest mane/tail/horn within the preview bounds. Preserve all current unlocked/owned cosmetics and IDs.

Implementation:

- Introduce a creator draft model and explicit control descriptors. The renderer reads draft state; controls update it through commands.
- Remove old-coordinate category capture, duplicate controls and rendered-scale selection inference when the new owner replaces them.
- Save once on explicit confirmation; Back/Cancel preserves original profile and adventure progress.
- Keep gameplay hotkeys disabled while typing, including E/S/Space/Enter as appropriate to name entry.

Validation: every option/category in new/edit mode; 16-character name, blank/whitespace normalisation, maximum preview extents, rapid changes, randomise/reset/cancel, keyboard focus, touch, rotation, soft keyboard and repeated scene entry. Compare saved appearance after continue.

Human gate: approve actual creator screenshots/interactive preview before adoption. Done when no overlap/hidden option exists across the matrix and the user can understand the path without a paragraph of instructions. Backout: creator-only revert; draft changes do not alter save schema.


## Standard technical gate

Run `npm run validate`, the relevant existing/new browser specs with `npm run test:play -- <spec paths>`, and `npm run test:browser-compat`. Review full serial Chromium evidence before closing implementation packages as required by TESTING.md; WP19I must run all gates on the integrated candidate. Preserve the 520 KiB entry budget and save compatibility. Record commands, commit, skips and outcomes separately from human acceptance.

## Non-goals and delivery

No unrelated world expansion, new save schema, race steering, engine rewrite or production deployment. Use a package branch and draft PR. Do not merge across the stated human gate. Update STATUS.md, PROJECT_STATE.json and the feedback ledger with actual evidence.
