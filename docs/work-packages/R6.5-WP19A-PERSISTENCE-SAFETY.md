---
id: R6.5-WP19A
title: Persistence and truthful success feedback
status: in_progress
autonomy: green
depends_on: []
parallel_safe: false
human_gate: none
---

# R6.5-WP19A - Persistence and truthful success feedback

Approved by David on 2026-09-08 as part of the remediation plan. Begin only when this package's dependencies and applicable human gates permit.

## Governing contract

Read `docs/2026-09-08-REMEDIATION-PROPOSAL.md`, especially Shared acceptance contract, Roadmap reconciliation and Delivery and resumption. The original feedback ledger and accepted WP18I/J shell remain protected. The section below is the bounded scope; the proposal is authoritative if duplicated wording differs.

## Scope and execution contract persistence and truthful success feedback

Autonomy after plan approval: Green for the defined defect fix. Dependencies: none beyond current accepted baseline. Findings: A09/A10.

Scope:

- Catch storage access/read failures at a deliberate boundary and expose a typed recoverable outcome.
- Preserve valid existing data when storage is unavailable. Do not treat unreadable as empty or corrupt as permission to reset.
- Make purchase/collection/reward success depend on a committed save result. Inspect Bakery, Shop, Inventory, Shimmer, quest effects and home decoration callers.
- Emit success events exactly once after successful persistence; failed writes must not advance dependent quests or show a successful purchase.
- Retain authoritative checkpoint recovery and future-version protection. No save-schema redesign.
- Show concise retry/recovery feedback without leaking technical storage details into the child's normal flow.

Validation:

- Regressions for denied localStorage access/read, full quota, failed checkpoint write, valid checkpoint with failed primary write, corrupted primary with backup, future schema and failed new-game reset.
- Reproduce the three-Shimmer/Berry-Bun case: failure produces no purchased result, no inventory loss, no false balance, no quest event; retry after recovery succeeds once.
- Existing persistence and economy suites plus new event-order assertions; new/continue/redesign smoke.

Done when no caller reports committed success for a rejected write, normal saving remains compatible, and recovery never silently overwrites unreadable progress. Backout: revert package code; no migration required.


## Standard technical gate

Run `npm run validate`, the relevant existing/new browser specs with `npm run test:play -- <spec paths>`, and `npm run test:browser-compat`. Review full serial Chromium evidence before closing implementation packages as required by TESTING.md; WP19I must run all gates on the integrated candidate. Preserve the 520 KiB entry budget and save compatibility. Record commands, commit, skips and outcomes separately from human acceptance.

## Non-goals and delivery

No unrelated world expansion, new save schema, race steering, engine rewrite or production deployment. Use a package branch and draft PR. Do not merge across the stated human gate. Update STATUS.md, PROJECT_STATE.json and the feedback ledger with actual evidence.

## Dispatch

David authorised the project-manager/Codex execution workflow on 2026-09-08. This package is prepared for dispatch on `agent/r6.5-wp19a-persistence-safety`, based on approved plan PR #159. The task comment and Codex reply are the evidence of requested/accepted execution; branch creation alone is not proof a worker started.

Dispatch result: PR #160 task comment 5584158968 initially received Codex bot response 5584160637 requesting a repository cloud environment. David then confirmed “environment made” and authorised one retry; implementation ran on the existing PR head without creating a duplicate PR.

## Implementation evidence, 8 September 2026

- Browser storage getter access is deferred into the repository boundary. `SaveService.loadWithResult()` distinguishes loaded, empty, future-version-blocked and storage-failed reads; legacy mutating callers receive a recoverable storage error rather than treating unreadable progress as a fresh game.
- `SaveService.save()` now rejects failed commits. The write-ahead schema checkpoint remains authoritative: a completed checkpoint is saved even if the secondary primary write fails.
- Bakery and Shop return `persistence-failed`, keep the previous balance/ownership and show concise retry copy. Bakery collection events occur only after `SAVE_COMPLETED`. Existing Inventory, Shimmer, quest and decoration mutation paths stop before their success events/results when `save()` rejects.
- Focused fault tests cover denied reads, corrupt-primary backup recovery, valid future checkpoints, checkpoint failure, secondary-primary failure, three-Shimmer/Berry-Bun failure and one successful retry, plus Inventory/Shimmer event suppression. Existing new-game reset tests retain failed-checkpoint coverage.

Validation on the implementation worktree:

- `npm run validate`: passed (114 test files / 433 tests, build, 515.29 KiB entry budget and static smoke). Existing lint diagnostics remained warnings and were not weakened.
- `npx vitest run src/game/save/SaveService.storageFaults.test.ts`: passed, 6 tests.
- Required targeted Chromium smoke, full serial Chromium and browser compatibility commands were attempted after downloading the matching browsers. They could not launch because this runner lacks Playwright host libraries; `npx playwright install-deps` could not obtain them from the configured apt sources. These are environment failures, not the historical stale-assertion baseline recorded by the audit, and are not claimed green.
- PR Validate and Cloudflare deployment checks passed for implementation commit `79cf7fa`; browser playtest/compatibility remained running at handoff. Cloudflare had not posted an exact immutable preview URL for that commit. The earlier `https://deef9ebf.unicorn-valley.pages.dev` preview belongs to setup commit `c898bfb`, not this implementation.
