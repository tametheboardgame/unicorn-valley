---
id: R6.5-WP18C
title: Landscape Tablet Controls and Interaction Shell
status: complete
autonomy: amber
depends_on: [R6.5-WP18B]
parallel_safe: false
human_gate: none
---

# R6.5-WP18C - Landscape Tablet Controls and Interaction Shell

## Objective

Implement the human-approved WP18A landscape-tablet shell and control direction as a shared system, replacing the current pattern where portrait touch receives special treatment while landscape tablets mostly inherit desktop/canvas chrome.

## Design authority

- Approved WP18A concept images and recorded decisions.
- `docs/07Z-R6.5-WP17-PLAYTEST-REMEDIATION.md`.
- Samsung Galaxy Tab S8, Chrome, landscape as the reference human-test environment.

If code convenience conflicts with the approved concept, preserve the approved interaction intent unless a technical limitation is documented and the user approves a deviation.

## Core implementation requirements

### Tablet-mode detection

Create a coherent tablet/touch presentation decision that does not equate "mobile" with narrow portrait width.

Use appropriate signals such as coarse/touch pointers, orientation and effective viewport/aspect constraints. Avoid brittle device-model user-agent rules.

### Exploration controls

- Preserve tap-to-move as a first-class movement method.
- Keep directional movement available without letting it dominate the screen.
- Place touch controls in comfortable landscape thumb zones according to the approved concept.
- Rework Gallop according to the approved hold/toggle/placement decision.
- Support simultaneous movement/action input safely where the chosen control design requires it.
- Prevent stuck axes/buttons after pointer cancel, pointer leave, scene pause, modal launch or focus loss.

### Contextual primary action

Provide one unmistakable large contextual action surface when an action is available, e.g. Talk to Pip, Enter Shop, Pick Up, Examine or Start Race.

- The label must state the actual action.
- Direct tapping of an eligible NPC/interactable should also work where spatially sensible.
- Direct tap and contextual action must invoke the same authoritative interaction path, not duplicate bespoke logic.
- Automatic gateways/transitions remain automatic only where that is deliberately designed.

### Permanent shell/chrome

Implement the approved compact treatment for:

- Bag;
- Map;
- Wonderbook;
- Settings/Pause;
- location identity;
- currency/reward feedback;
- contextual hints.

Remove or collapse desktop-oriented permanent help chrome that contributes to clutter.

### Device-appropriate help

Child-facing help must describe the controls present on the current device.

On landscape tablet do not lead with WASD/keyboard instructions. Desktop may still show keyboard guidance when appropriate.

### Layout safety

Create shared layout/safe-zone rules so shell components do not overlap each other or critical gameplay.

- No silent text clipping.
- Text wraps, truncates deliberately with a usable affordance, or scrolls.
- Primary touch targets are generous and consistent.
- The gameplay world remains the dominant visual surface.

## Race-control integration boundary

WP18C establishes the common tablet interaction/control primitives and applies the approved race-control treatment where shared infrastructure is required. Race-specific gameplay regressions remain eligible for WP18D.

The approved race surface is RUN lower-left and JUMP lower-right only. There is no left/right steering and no separate race Gallop. RUN and JUMP must work simultaneously, the track must stay visually dominant, and no required action may depend on a Chrome long-press gesture.

## Regression coverage

Browser coverage exercises the real tablet-facing flows, including:

- landscape touch/tablet shell selection;
- Map, Bag, Book and Settings access;
- tap-to-move;
- directional controls and Gallop touch targets;
- contextual action visibility and direct interaction targeting;
- safe input release on blur/pause;
- simultaneous RUN + JUMP multi-touch;
- replacement race Pause/Resume/assistance controls;
- preservation of portrait and desktop secondary paths.

## Preserve

- desktop keyboard/mouse support;
- existing save/progression authority;
- strong point-and-click behaviour;
- 520 KiB application-entry budget;
- browser compatibility suite.

## Delivery evidence

WP18C delivered:

- shared capability/viewport-based landscape-tablet presentation detection without device-model UA rules;
- compact top Map / Bag / Book / Settings navigation, Shimmer status and location identity;
- large lower-left four-direction exploration pad and separate Gallop control;
- large lower-right contextual action plus bottom-centre child-facing hint;
- direct target tapping routed through the existing authoritative interaction input path;
- stuck-input release hardening across pointer cancellation, scene pause, focus loss and visibility loss;
- direct Map launch and full Settings access from the shared exploration shell;
- race controls changed to the approved RUN lower-left / JUMP lower-right layout only;
- removal of the landscape race bottom control deck that previously shrank/obscured the track;
- long-press/context-callout suppression and reliable RUN hold semantics;
- simultaneous RUN + JUMP coverage;
- explicit race Pause/Resume and assistance flow on landscape tablet;
- corrected durable WP18A/decision wording reflecting the user's no-steering race clarification.

Exact-head technical validation on implementation head `456f721c78bbfe1ccf734745415dd380c48763b3` is green:

- AI project operating contract: pass;
- formatting, lint and type-check: pass;
- unit tests: pass;
- production build and static smoke: pass;
- hard **520 KiB** application-entry budget: pass;
- Chromium/Firefox/WebKit compatibility: pass;
- full Chromium playtest: **150 passed, 3 intentionally skipped**;
- target-tablet creator/exploration/Book/accessibility flow: pass;
- target-tablet simultaneous RUN + JUMP flow: pass;
- target-tablet race Pause/Resume/assistance flow: pass;
- WP18B Beach/Hollow Tree/Twinkle stability regressions remain green.

The final automated playtest reported zero errors and zero warnings. Its two non-blocking suggestions concern high decorative object counts in Moonflower Glade and Rainbow Meadow and remain candidates for later tablet-hardening/performance work if device evidence warrants it.

## Acceptance

Complete. The approved shell now exists as reusable production infrastructure, tablet exploration and race controls follow the approved hierarchy, required race input no longer depends on Chrome long-press behaviour, and exact-head automated validation is green. Human confirmation of the complete remediation remains deliberately deferred to WP18H.

## Exit

Move to `R6.5-WP18D - Playthrough Functional Bug and Regression Remediation`.
