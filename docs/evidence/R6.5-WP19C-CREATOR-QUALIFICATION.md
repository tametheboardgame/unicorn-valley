# R6.5-WP19C creator qualification

Date: 9 September 2026.

## Release prerequisite

WP19B PR #162 merged as main `c2d98ae3287cc8ab6b2548f5efbf5548b7d87979`. Cloudflare associated that exact SHA with successful immutable production deployment `https://39034745.unicorn-valley.pages.dev`. Existing supported Actions run `34356744951`, job `102483245996`, passed isolated startup, creator save, reload and Continue against the production alias while checking out that merge SHA. The local workflow-dispatch token returned HTTP 403, so controls were not bypassed; the existing PR-triggered route was bound to the exact production candidate instead.

## Implementation contract

The creator now owns a detached draft and explicit named control descriptors. Desktop and touch landscape use the six approved progressive categories and reveal only the selected category. Portrait uses the same six categories in a scrollable DOM surface, with its name and primary action in document flow. Selection state is read from the draft owner rather than inferred from rendered scale. Randomise preserves an existing name; reset and restore are distinct; no save occurs until the explicit confirmation command.

The profile appearance schema and every existing cosmetic ID remain unchanged. The same `UnicornAppearance` value feeds the preview renderer and persisted world renderer.

## Evidence matrix

Fresh local captures are in `docs/evidence/wp19c-current-head/` for desktop 1280×800, landscape tablet 1024×768 and landscape phone 844×390. The same capture set includes portrait phone 390×844; all four files were checked as valid PNGs and visually inspected. Browser automation and compatibility remain authoritative for interaction.

## Checks so far

- `npm run validate`: passed 116 files / 443 tests, production build/static smoke and 481.58 KiB under the unchanged 520 KiB entry budget. Lint reported 40 existing warnings and no errors.
- `npx vitest run src/game/ui/CreatorProgressiveModel.test.ts`: 4 passed.
- focused landscape progressive test: 1 passed.
- Creator Plus regression rerun: 2 passed, 1 timed out during the enlarged real-control sequence on the slow local software renderer; the assertion failure was not waived and its bounded timeout has been adjusted before rerun.
- Production prerequisite smoke: passed.

Technical qualification, exact-head CI, compatibility, four-class completeness and immutable WP19C preview remain pending. Human visual acceptance remains not ready.

## Exact-head CI result

CI `34359405200` passed Validate, browser compatibility and browser shards 1/3 and 3/3; browser shard 2/3 failed. GitHub exposes only its exit-code annotation and returns HTTP 403 for both the job log and uploaded artifacts from this runner, so the failing case is not guessed or waived. The project-contract check passes on the corrected state head. WP19C is not technically qualified until shard 2/3 is diagnosed, corrected if current-work related, and exact-head CI is green.

## Approved-concept implementation update

David rejected the earlier capture set and approved `docs/design/wp19c-approved-creator-concept.jpg` instead. The replacement uses separate Colours, Mane, Tail, Horn, Markings and Accessories tabs; pencil-only landscape renaming; direct large supported-style cards rendered by `drawUnicornAppearance`; large swatches; and an aligned Back/confirmation footer. It deliberately does not reproduce the concept's presentation casing or unsupported proposed horn artwork. The material art difference is explicit: the game retains its established procedural unicorn rather than the concept's painted promotional illustration, while preview, cards and world all use the real shared appearance renderer.

Fresh four-class captures at `docs/evidence/wp19c-current-head/` show the Horn card panel at desktop/tablet/phone landscape and portrait. They were visually inspected against the approved concept on 9 September 2026: the inner hierarchy, palette, selection state, spacing and action grouping are close; the supported procedural art is simpler than the painted reference. `npm run validate` passes 116 files / 443 tests with a 481.71 KiB entry bundle. The focused desktop real-input create/save journey passes. The first focused portrait rerun exposed and fixed disabled direct swatches; its enlarged slow-software-renderer rerun reached the total timeout without an assertion message, so portrait interaction and full compatibility remain pending exact-head CI rather than waived. The historical shard-2 failure belongs to the superseded head and must be replaced by a new exact-head run.

## David layout-correction recovery

David's four original tablet captures in `docs/evidence/wp19c-david-layout-feedback/` override the earlier visual-pass claim. The corrected implementation now renders isolated supported components instead of whole-unicorn cards, keeps all grids within the options panel, separates swatches from the footer, preserves tab/card state after pointer exit and draft replacement, synchronises programmatic name changes, exposes a focusable DOM pencil and reapplies its owner after every scene activation. Legacy `LIVE PREVIEW`, name-banner and preview-shadow objects are explicitly named and retired by the progressive owner.

Corrected local captures are `tablet-landscape-corrected.png`, `phone-landscape-corrected.png` and `phone-portrait-corrected.png` in `docs/evidence/wp19c-current-head/`. Visual inspection confirms the centred heading, clean Colours groups, unobstructed footer and full preview. The portrait capture shows the intended scrollable DOM controls; options below the fold remain reachable rather than being compressed. Category-by-category corrected evidence, immutable preview and exact-head CI are still pending and are not represented as complete.

## Category-complete correction evidence

The reported 1536×960 browser viewport was reproduced with the actual 1280×720 game area. All six selected-tab states are captured in `docs/evidence/wp19c-current-head/tablet-categories/` (`colours.png`, `accessories.png`, `mane.png`, `tail.png`, `horn.png` and `markings.png`). Visual inspection against David's four originals and the approved concept confirms: both panels use the available game area; the heading spans the creator; the name surface is cream with one pencil; `LIVE PREVIEW` is absent; Colours contains only labelled Body/Eye swatches; every style card contains only its relevant supported component; grids and their hit areas remain inside the panel; mane/tail swatches remain above a separate unobstructed footer; and exactly one tab remains gold through the complete Colours → Accessories → Mane → Tail → Horn → Markings sequence.

CI `34398055366` was bound to `78a92176af9c24a857942804fe2ffda5022feb8d`. Validate, compatibility and shard 1/3 passed; shards 2/3 and 3/3 failed. GitHub's signed log and artifact storage still returns HTTP 403, so the failures were reproduced with the same shard commands locally rather than waived. Shard 2 exposed two obsolete WP6.12 profile-redesign steps which attempted to fill the deliberately hidden pencil-only input and select a retired legacy swatch name; its creator regression now activates the focusable pencil and selects the progressive card. The WP18E progressive journey likewise no longer waits for retired arrow controls and now traverses all six current categories before returning to Colours. A simultaneous slow software-renderer run also produced unrelated 45-second timeouts in title and cross-region cases; those are not represented as product failures. Replacement exact-head CI remains required.

## Replacement CI 34404549752 diagnosis

CI `34404549752` was bound to `8636b9715e5e6fc27ba3e8280ead3d5f1f3a5b18`. Validate and browser compatibility passed, but all three numbered Chromium shards failed. Signed job logs and all three report archives again returned HTTP 403, so the shard allocations were listed and the affected groups were reproduced locally. Shard 1 exposed the real Nova countdown still at `1` when its 25-second wait expired on a capped-delta software renderer. Shard 2 exposed the portrait title journey exhausting its 45-second total after completing its retained geometry checks, plus the invalid-cosmetic regression expecting Mane and Accessory labels while Colours was intentionally the only visible category. Shard 3 exposed two WP18G assumptions that the pencil-only input was always visible, and also showed that the 45-logical-pixel category tabs rendered at 36 CSS pixels in the 1024-wide 4:3 case.

The correction preserves coverage: it waits longer for the real countdown and full portrait settings journey; verifies fallback values from the creator's draft-backed named controls; activates and focuses the keyboard-accessible pencil before editing; checks all six category targets including Tail; and enlarges landscape category tabs to retain a 40 CSS-pixel minimum at the 4:3 scale. `npm run validate` passes with 116 files / 443 tests and a 483.66 KiB entry bundle. Focused local reruns confirmed the portrait title journey and desktop pencil journey; the unusually slow local software renderer required the deliberately enlarged totals recorded in the tests. Replacement exact-head CI and candidate-bound deployed smoke remain required and are not claimed in advance.

The first correction run, CI `34441843861` at `7ccf72d4c66f8e15acd58926e5b7f6b4ed574049`, passed Validate, compatibility and shards 1/3 and 3/3. Shard 2/3 still failed. Its signed report remained inaccessible, while the completed bounded local shard output exposed the remaining current-creator cases: a second resilience regression expected three hidden-category labels simultaneously, and the tablet touch journey included the intentionally retired canvas name label among touch targets before tapping the old confirmation coordinate. The follow-up reads draft-backed fallback values from named controls, leaves the name label display-only because the real DOM pencil owns input, increases swatches to a 48-logical-pixel touch diameter, and taps the named confirmation owner. Other local failures from the concurrently overloaded software renderer were timeout-only and are not substituted for the single CI failure. A further exact-head run is required.
