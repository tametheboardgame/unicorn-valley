# WP19H0 H0A candidate retirement ledger

H0A identifies candidates; it deletes nothing. Every item requires H0J caller/producer
proof, parity tests and an independently revertible migration.

| Candidate | Caller/producer evidence | Why it is a candidate | Proof required before retirement |
| --- | --- | --- | --- |
| `LegacyWorldFeedbackMigrationManager` | Constructed in `src/main.ts`; scans active scene children for a finite list of legacy top-text signatures. Its class comment says it is transitional until producers use shared presentation. | Per-frame/scene scanning compatibility layer after `WorldFeedbackPresenter` adoption. | Enumerate every recognised signature and producer; migrate producers; regression-test discovery/collection distinction; assert no new imports/signatures. |
| `DesktopConceptCleanupManager` | Immediately imported in `src/main.ts`; searches scene children by text/name/position to hide old location, instruction, shadow, prompt and Beach HUD objects. | Accepted shell is canonical, but old scene objects are still produced then hidden. | Remove each producer at source and prove desktop/tablet/phone parity plus no hidden input owner remains. |
| `PortraitConceptPresentationManager.suppressLegacy` | Bootstrap-scheduled; scans named objects and positional chrome before adding a separate DOM shell. | Parallel presentation suppresses rather than removes scene-owned legacy output. | Establish H0C overlay/shell contract, remove proven duplicate producers, preserve rotate/re-entry behaviour. |
| `CoreSceneInteractionBridge` prototype capture | Patched before Phaser game construction; captures four scene prototype activators and disables legacy prompts/input. | Transitional adapter around scene-owned interaction methods after coordinator adoption. | Trace all providers/callers, move activation to canonical registry/coordinator and retain input-consumption/close suppression coverage. |
| `LandscapeCreatorProgressiveManager.hideLegacyControls` and creator legacy objects | Manager is constructed in `src/main.ts`; `UnicornCreatorScene` still creates objects named `creator-legacy-*` which the manager hides/recomposes. | Two creator presentations exist simultaneously. | Prove saved IDs, reset/randomise/rename, responsive and re-entry parity before moving/removing producers. |
| `R6FinalPlaythroughCleanupManager` | Bootstrap import group in `src/main.ts`; finds/hides named legacy paths, dividers and gateway labels. | Release-era cleanup remains permanent global runtime work. | Locate each named producer, confirm accepted replacement geometry, remove producer and update focused geometry tests. |
| `FinalGraphicsTighteningManager` legacy stream suppression | Bootstrap import group; finds and hides a legacy stream in active scenes. | Presentation patch is keyed to an obsolete scene object. | Identify producer and accepted replacement; visual/geometry regression evidence at supported layouts. |
| `ExplorationGeometryPresentationManager.replaceLegacyHint` | Synchronous composition-root owner; searches for legacy hint text and replaces it. | Runtime replacement indicates duplicate ownership. | Migrate hint producer to canonical shell/message owner and cover interaction wording/visibility. |
| `WorldTraversalPolishManager.hideLegacyGatewayObjects` | Instantiated transitively by world systems; scans and hides old gateway labels. | Another cleanup-by-scanning path overlapping final cleanup and gateway owners. | Map gateway producer names and consolidate only after navigation/topology parity evidence. |
| `VillageInteriorScene` / `R6VillageInteriorScene` duplicate key | `gameConfig` statically registers the former as `VillageInteriorScene`; `src/main.ts` imports the latter and only adds it if that key is absent. `VillageInteriorContractManager` is also globally constructed. | Two implementations claim one stable key and registration order determines reachability. | Establish which implementation is live on every entry/continue path, compare behaviour/content/tests, then retain one without changing the stable key or return semantics. |
| Bootstrap-immediate dynamic registration imports | `src/main.ts` schedules Inventory/Wonderbook/Shop, five micro interiors and most world/content managers immediately. | Code splitting currently obscures lifecycle/readiness: many “lazy” chunks are startup requests. | H0D/H0H manifest and readiness policy; measure network/first-playable effects before moving boundaries. |
| WP-numbered browser organisation and coordinate/index assertions | 71 play files; machine inventory flags six obvious coordinate/index-sensitive files, while many filenames encode delivery history. | Test intent/owner is difficult to discover and selected assertions may protect implementation detail. | H0E contract-by-contract review; replacement test or explicit obsolete behaviour evidence for every removal. |

Save migrations, fallbacks for retired item IDs and legacy-save repair are **not**
retirement candidates merely because they mention “legacy”. They protect the frozen
backwards-compatibility boundary and remain until a separately authorised migration
changes that contract.
