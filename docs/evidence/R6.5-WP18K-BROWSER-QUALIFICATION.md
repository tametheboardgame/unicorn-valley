# R6.5-WP18K browser qualification ledger

Evidence date: 8 September 2026. Initial candidate: `a958d0c20ba5c286ca1f3ee56022c30826e7954e` on draft PR #161. Exact-head CI run: `34262336206`; browser playtest job: `102183289829`; compatibility job: `102183289834`. Exact immutable deployment: `https://51d8c0eb.unicorn-valley.pages.dev` (Cloudflare reported successful for `a958d0c`).

## Qualification conclusion

Exact qualified candidate `c4697e8647389f37e773f9aad529ebb849ae1764` CI run `34300718194` completed all **182 dispositions: 179 passed, 0 failed and 3 configured skips**, with **0 unrun**. Shard 1, shard 2 and shard 3 all passed; browser compatibility passed **48 / 15 configured skips**, and Validate/project-contract passed. The prior `5f13cc3` run completed 176 passed / 3 failed / 3 configured skips / 0 unrun and is retained below as the correction input. Shard 1 reported 60 passed / 1 failed; shard 2 reported 56 passed / 2 failed / 3 configured skips; shard 3 reported 60 passed. Validate, browser compatibility (**48 passed / 15 configured skips**) and project-contract run `34292365355` passed. This is not a green run: the Nova return, Lumi advancing-guidance and landscape race-control geometry cases remained required.

Cloudflare published exact `5f13cc3` at `https://55e7e35f.unicorn-valley.pages.dev`. K5 follow-up traced each remaining failure rather than carrying forward an unchanged-scene classification: the Nova case blocked while walking from the default spawn and waiting for retired visible copy; the Lumi hint was incorrectly hidden by the cleanup manager's broad legacy-input scanner; and the race assertion applied portrait below-canvas wrapper geometry to the accepted landscape overlay. The bounded corrections and replacement assertions are recorded in the final section below.

## Nine completed CI failures

Each failure was compared with the `d8f3de6` baseline, the WP18K diff, its asserted owner and local trace/result. “Stale” means the assertion names a retired implementation while replacement coverage retains the child-visible behaviour.

| # | CI failure | Ownership comparison and classification | Disposition |
| ---: | --- | --- | --- |
| 1 | Nova identity/conversation return | `RainbowMeadowScene`/`NovaStoryScene` and dialogue return code are unchanged by WP18K. The 75-second total budget expired after entering the conversation route; adjacent Nova race-return coverage passed. | Not caused by K2–K4; retained for WP19D/E/I, with no conversation redesign absorbed here. |
| 2 | Exploration Settings return | After rows regained the required 64 logical-pixel height, the clipped Reduced Motion row correctly disabled pointer input until fully visible. The test incorrectly required every offscreen row to be interactive. | Scroll the real viewport first, then require visible, interactive 64-pixel accessibility targets; retain music/high-visibility persistence and return/reload proof. |
| 3 | Core dialogue portrait | `LumiStoryScene` and dialogue production presentation are unchanged by WP18K. | Outside consolidation; retained for WP19E/I rather than weakening portrait guidance coverage. |
| 4 | Reduced Motion production UI | The test expected an Inventory sparkle although the accepted baseline `UiProductionPresentationManager` has long owned only Shop/Decorate and Inventory is now scene-owned. | Stale owner corrected to the surviving Shop decoration; both fixed-alpha samples remain asserted. |
| 5 | Landscape race bar | `RaceMobileControlsManager`, race DOM and race authority are unchanged by WP18K. | Outside K ownership; retain ≥90 bar and ≥74 action target assertions for WP19I. |
| 6 | Core modal shell | The test expected old production anchors on Inventory/Wonderbook. Inventory now owns its shell directly; Shop intentionally retains production ornaments; Wonderbook owns its page surface. | WP18K-relevant owner assertions corrected without restoring wrappers. |
| 7 | Wonderbook tabs | Diagnostics measured the 33-pixel text label, which was also the pointer target. This was a genuine undersized effective target despite the accepted label styling. | Preserve the canonical page-edge labels and add separate transparent 60-logical-pixel-high hit rectangles (48 rendered CSS pixels at the 0.8 supported scale). Retain real All/Secrets navigation and ≥48 geometry assertions. |
| 8 | Dialogue/sound settings states | The dialogue half is unchanged; the sound half queried retired world audio controls rather than current `SettingsScene` rows. | Settings half rebased to canonical navigation and four interactive sound rows; dialogue assertion retained. |
| 9 | Target-tablet creator/exploration/Book/accessibility journey | The composite used old fixed coordinates after Settings row reflow, tapping no controls and persisting `{}`. | Scroll the actual Settings viewport and tap named, visible interactive Reduced Motion and High Visibility controls; retain creator/Book/navigation sizes, save proof and reduced-motion NPC stability. |

## Forty cases unfinished by CI

The four local groups below cover every CI case 143–182. “Initial” records the first bounded result on `a958d0c`; corrected reruns are recorded separately so failures never disappear from the evidence.

| CI cases | Bounded group | Initial result and disposition |
| --- | --- | --- |
| 143–144 | Nook and Twinkle eight-cycle lifecycle | Nook exceeded 120 seconds on the final return transition; Twinkle was the one explicit serial skip following that failure. No cycle/assertion was removed. Nook's complete eight-cycle budget is now 240 seconds and Twinkle's 180 seconds, based on retained traces and software-rendered Chromium timings. |
| 145–147 | Food/Map/Creator | Map passed. Food and Creator failed on obsolete hard-coded coordinates. Named interactive-owner clicks now preserve scroll, explicit Food use/persisted decrement, Map distinction and every progressive category assertion; corrected Food, Map and Creator rerun **3/3 passed**. |
| 148–152 | WP18F visual scenes | **5/5 passed**: Meadow/Nova bodies, Shell Cove, Grotto, Village shops and Woods atmosphere. |
| 153–158 | WP18G hardening | **4 passed, 2 failed** initially. Browser containment, exploration matrix, rotation and desktop secondary input passed. The composite modal matrix exposed a real 36 CSS-pixel Bag tab at 1024×768; WP18K restores ≥48 CSS pixels rather than lowering the assertion. Race controls reached the final locator at 45 seconds; its complete two-size geometry assertions retain a 75-second budget. |
| 159–165 | WP18I concept UI | **2 passed, 5 failed** initially. HUD hierarchy and real Talk/Enter passed. Matrix and Decor/Race exhausted total budgets; Settings tests directly started an intentionally lazy/unregistered scene; desktop expected deleted `desktop-concept-*` objects. Tests now open Settings through canonical HUD navigation, assert the overlay owner and use bounded multi-surface budgets. |
| 166–172 | WP18J preservation | **7/7 passed**: isolated HUD camera, Bag cross/pockets, Map cross/pan, short landscape, rotation restore, modal round-trip geometry and Map clipping/North. |
| 173–182 | WP5–WP9 world depth | **10/10 passed**: Glade/Cottage/Nook, Meadow/Lookout, Brook/Grotto, Woods/Grove, Beach and Woods–Beach gateway. |

## Compatibility skips

The 15 skips are the same three deliberately superseded portrait contracts in each of five non-mobile projects (Chromium desktop, Firefox desktop, WebKit desktop, Chromium tablet-touch and WebKit tablet-touch): old portrait dock placement, old grouped creator controls and old large “Talk to Pip” presentation. All three execute and pass in both portrait mobile projects, producing six of the 48 compatibility passes. Their product replacements remain owned by WP19C and WP19D; they are configured skips, not passes and not automatic WP18K exceptions.

## Preserved package evidence

- Exact-head `npm run validate`: 114 test files / 435 tests, production build/static smoke, and 480.01 KiB entry under the unchanged 520 KiB limit.
- Exact-head compatibility: 48 passed / 15 configured skips across Chromium, Firefox and WebKit desktop plus tablet/mobile touch projects.
- Exact-head CI playtest: 130 passed / 9 failed / 3 skipped / 40 unfinished; case 141 ten-cycle Beach Bag passed in 1.7 minutes, case 142 retired item ID passed, and all save-recovery cases 117–121 passed.
- Bounded unfinished-case run: every case 143–182 attempted and explicitly disposed above; first pass 29 passed / 10 failed / 1 serial skip.
- Immutable deployment evidence: Cloudflare successful for exact starting head `a958d0c` at `https://51d8c0eb.unicorn-valley.pages.dev`. A new immutable URL must be verified for the correction head; no generated URL is inferred.


## Correction-head failure disposition and CI grouping

The ten failures are individually retained: Settings pause/accessibility, target-tablet accessibility, Wonderbook target geometry and desktop/concept HUD owner waits are K-related corrections; Nova conversation timeout, core dialogue mode hint and landscape race DOM placement are unchanged/transitive findings assigned to WP19D/E/I; the Bag/Settings and Decor/Race composite timeouts are split by the repository's three-way Playwright sharding rather than weakened or skipped. The Settings scroll assertion now evaluates fully interactive row bounds rather than partially clipped visible label glyphs.

CI now runs the complete Playwright suite as three deterministic shards. Every test remains selected exactly once; `fail-fast: false` lets all shards report, each failing shard fails the aggregate required check, and there is no `continue-on-error`. Per-shard artifacts preserve traces and screenshots. The existing three playtest skips and all 15 compatibility skips remain unchanged.

## Supported deployment smoke route

The local runner cannot perform the remote Pages smoke because its authorised proxy rejects Cloudflare CONNECT. That limitation was not extrapolated to GitHub Actions: the bounded existing-Actions route below used ordinary network access and ultimately passed startup/save/reload/Continue against an immutable preview. No proxy, allowlist, paid service or bypass was introduced.

## Current four-class visual evidence

Current candidate evidence is generated by `scripts/captureWp18kEvidence.mjs` under `docs/evidence/wp18k-current-head/`: four screenshots per display class (`*-hud.png`, `*-settings.png`, `*-bag.png`, `*-book.png`) and rendered `geometry.json`. These current implementation captures supersede the retained historical `wp18k-screenshots` files for manager review; neither set is physical human acceptance.

## Exact `f80271d` sharded result and bounded follow-up

Run `34283666644` completed within the job deadline and classified all 182 cases: shard 1 was **58 passed / 2 failed / 1 unrun**, shard 2 **55 passed / 3 failed / 3 configured skips**, and shard 3 **58 passed / 2 failed**, for **171 passed / 7 failed / 3 configured skips / 1 serially unrun** overall. Validate, compatibility (**48 passed / 15 configured skips**) and project-contract passed. Cloudflare published exact `f80271d` at `https://a62d4c47.unicorn-valley.pages.dev`.

The bounded correction addresses four K-owned causes without weakening coverage: Settings now activates the named keyboard-focused High Visibility row and re-scrolls it after reload; Cottage Decorate starts through a valid centre-rug slot before the retained Race checks; nested legacy prompt text is evaluated with ancestor visibility and suppressed recursively (the captured visible `Sit: Cottage step · E / Enter / tap` was a genuine cleanup gap); and Nova first-run follows the visible story decision rather than assuming a direct race route. Wonderbook's transparent targets are now 60 logical pixels, yielding 48 CSS pixels at the supported 0.8 scale; the test measures rendered canvas scale rather than calling logical pixels CSS pixels.

The serially unrun Sunrise Sprint case was rerun independently. It reached the real race and visible manual controls, but the local software renderer advanced only one countdown step during the retained 25-second startup wait; it remains a non-green local timing result pending exact-head Actions, not a skip or pass. The unchanged Nova conversation-return timeout, Lumi mode-hint visibility and landscape race geometry remain required assertions with WP19D/E/I named as later functional owners; they are not accepted under WP19A and current-head CI must disposition them.


## Final three-case correction

- **Nova identity and return:** the failing call was `approachVisiblePrompt(..., 'Talk to Nova')`, which coupled a long default-spawn walk to copy intentionally replaced by the contextual action system. The test now uses the existing diagnostics positioning route to place the real player at Nova's canonical `(2470, 930)` interaction area, requires the named visible `Talk` contextual action and Nova-specific hint, enters through a real `E` edge, verifies canonical Nova identity, exits with Escape, and preserves both sub-pixel return-position assertions. It neither resurrects retired copy nor skips return coverage.
- **Lumi advance guidance:** `DialogueCard` still deliberately renders `Enter / tap to continue`, the visible arrow and the interactive Continue control. `DesktopConceptCleanupManager` mistakenly classified the named production hint as legacy input copy and hid it on the next post-step. Named `dialogue-production-*` controls are now exempt from that scanner. The original portrait, explicit guidance, indicator and real input assertions remain unchanged and the focused case passes.
- **Landscape race controls:** 844×390 touch uses the accepted landscape overlay by design, so the root wrapper correctly shares the canvas bounds (`y = 0`) rather than sitting below it. The test now verifies wrapper/canvas alignment, >=122 CSS-pixel RUN/JUMP targets in opposite corners, a clear central 28% track band, disabled duplicate canvas inputs, real RUN hold/release and simultaneous JUMP. Hidden landscape Help/Leave controls are no longer misrepresented as bottom-bar targets. Race mechanics are unchanged.
- **Release assurance:** `.github/workflows/deployment-smoke.yml` is a reusable manually dispatched, existing-Actions route; its PR #161-only bootstrap runs against exact delivery candidate `ebd524b` at `https://19e1602a.unicorn-valley.pages.dev` for an immutable `*.unicorn-valley.pages.dev` URL and exact candidate SHA. It uses a fresh browser context to start the deployed title, creates and saves an isolated `Actions Smoke` unicorn through the real creator, reloads, requires Continue and enters the restored Glade. It changes no real-player storage and makes no production deployment. The final Actions run and artifact are recorded after execution.
- **Current visual evidence:** `docs/evidence/wp18k-current-head/` contains current Bag, Settings, Book and HUD screenshots for desktop, tablet landscape, phone landscape and phone portrait plus `geometry.json`. The geometry file records candidate implementation SHA `781e12fc565003e7e5cd42932cf64ddf6f6d45c7`, viewport, rendered canvas, active scenes and named visible object bounds; it is the artifact index for delegated manager review.

The first authorised Actions execution, run `34300270226` / job `102305500818`, reached the immutable Pages host through normal network access and completed startup, creator save and reload. It then timed out waiting only for `MoonflowerGladeScene` after Continue; the saved default can canonically resume in Cottage. The route therefore was not denied, but the run is retained as a failed test rather than green. The follow-up accepts either supported saved exploration destination while still requiring the real Continue transition.

Follow-up Actions run `34300589233` again confirmed ordinary network access and completed the deployed flow in 14.6 seconds, but its transition predicate returned early for a non-title overlay before Title shutdown; the destination assertion therefore failed. The final check waits for Title itself to leave before evaluating the supported saved destination. Both failed attempts remain explicit qualification evidence rather than being called passes.


## Exact qualified-candidate result

Candidate `c4697e8647389f37e773f9aad529ebb849ae1764` passed Actions run `34300718194`: all three required browser shards passed, yielding **179 passed / 0 failed / 3 configured skips / 0 unrun = 182**; compatibility passed **48 / 15 configured skips**; Validate and project-contract passed. Cloudflare verified this exact candidate at `https://cc5b61db.unicorn-valley.pages.dev`.

The authorised deployed qualification succeeded in existing Actions run `34300718207`, job `102306837960`, in 53 seconds against immutable delivery candidate `ebd524b` at `https://19e1602a.unicorn-valley.pages.dev`. It used normal network access, isolated browser storage, real New Game/creator save, reload and Continue, and uploaded artifact `10084829132` (`immutable-deployment-smoke-pr-161-ebd524b`). The earlier failed runs `34300270226` and `34300589233` remain documented above.

Technical qualification is ready for delegated manager review. This does not claim delegated acceptance, merge, production deployment, physical WP18H replay or WP17 readiness.

## Manager visual-review correction

Manager review of all 16 first-pass images found evidence defects that green
tests missed. Candidate `d7cf5f46949e1aec35c994dc00915fe223595305`
keeps the 60-logical-pixel Wonderbook hit rectangles but exempts those
transparent targets from modal-surface restyling, restores the accepted
cross-only Book close with an invisible 82 × 70 logical target, and adds a
narrow regression covering readable labels, absent generated surfaces, real
filter navigation and the close presentation.

The refreshed evidence is indexed in
`docs/evidence/WP18K-VISUAL-REVIEW-INDEX.md`. It uses normal HUD entry, includes
empty and populated Bag, Settings before/after scroll, Map before/after drag,
Book/HUD, logical and actual CSS rectangles, and DOM companion rectangles across
all four classes. Exact baseline capture proves the small phone-portrait
Bag/Settings canvas and absent companions were already present at production
baseline `d8f3de6`; their actual dimensions are recorded rather than accepted as
child-sized. Approved WP19F owns the later consistent-modal work, subject to
manager disposition.

The deployment-smoke workflow no longer substitutes a hard-coded old preview
on pull-request events. It now requires explicit `deployment_url` and
`candidate_sha` inputs so the recorded immutable URL, deployed SHA and test
checkout SHA cannot silently diverge. Current correction-head CI, immutable
deployment and its deployed smoke remain pending at this documentation
checkpoint; the previous green run remains historical evidence only.

## Visual-correction requalification result

Immutable visual candidate `d7cf5f4` was published at
`https://f0490951.unicorn-valley.pages.dev`. Actions smoke run `34310313108`, job
`102335440694`, checked out that exact SHA and passed real startup, creator save,
reload and Continue in 53 seconds; artifact `10088127771` names the same SHA.
The workflow now reads the explicit candidate manifest rather than labelling an
old fallback preview current.

Exact-head CI did not become green and is not represented as qualified. Runs
`34309954791` and `34310313082` each had one shard-2 failure. Trace-led local
reproduction found sequential per-button Playwright calls exhausting the
portrait companion total while inspecting offscreen controls; the helper now
collects every target height/font metric atomically, retaining all assertions,
and the focused Wonderbook companion case passes. Run `34317210684` then
completed with shard 1 **60 passed / 1 failed**, shard 2 **57 passed / 1 failed /
3 configured skips**, and shard 3 **60 passed**, totalling **177 passed / 2
failed / 3 configured skips / 0 unrun**. Validate, compatibility (48 passed / 15
configured skips) and project-contract passed. Focused Wonderbook production,
portrait-companion and close/return cases pass locally, but the two remaining
CI failures could not be truthfully called green. The immutable manifest now
binds the latest runtime head `febfe2d` to verified preview
`https://890a12f5.unicorn-valley.pages.dev`; its ordinary Actions smoke is the
next authoritative result.

## Final tablet-journey correction

Exact documentation-head CI `34318525681` completed with Validate,
project-contract, compatibility and browser shards 1/3 and 3/3 passing. Browser
shard 2/3 recorded **57 passed / 1 failed / 3 configured skips**. The only
failure was the target-tablet creator/exploration/Book/accessibility journey,
which exhausted its existing 240-second total at `waitForScene` while waiting
for `MoonflowerGladeScene` after Book.

Source and interaction inspection identified a deterministic obsolete action,
not slow scene execution: the journey still tapped `(640, 682)`, the location of
the retired bottom “Close the book” button. The accepted visual correction moved
close ownership to the named, interactive top-right
`wonderbook-close-button`; the inert old coordinate therefore could not close
Book. The journey now dispatches the same real synthetic touch sequence to that
named current target. It retains creator targets, normal HUD entry, Map close,
player movement, contextual Talk, Book entry/return, scrolled Settings toggles,
persisted accessibility state and reduced-motion NPC checks; no timeout,
assertion, cycle or skip changed.

Exact correction-head `9e8c52e7fbd3bb7d31f03e4f651b97ce99e77189`
CI run `34322134793` passed Validate, all three browser shards and compatibility.
The complete browser disposition is **179 passed / 0 failed / 3 configured
skips / 0 unrun = 182**; compatibility is **48 passed / 15 configured skips**.
The formerly failing tablet journey passed in shard 2/3. Actions smoke
`34322134735` also passed against unchanged runtime candidate `febfe2d` and its
verified immutable preview `https://890a12f5.unicorn-valley.pages.dev`; artifact
`10092280469` records the isolated startup/save/reload/Continue result. Because
this correction changes only tests and durable documentation, no visual runtime
evidence refresh is required and the reviewed image index remains current for
`febfe2d`.

Technical qualification is ready for human review, not accepted. Under UV-D014,
David's sign-off checklist is: review the four-class image index; acknowledge
the inherited phone Bag/Settings target limitation assigned to WP19F; confirm
the 179/3 browser and 48/15 compatibility dispositions; confirm the immutable
preview and save/Continue smoke; then explicitly approve or reject WP18K. There
is no merge, production deployment or WP19B continuation authority before that
decision.

## Documentation-head Firefly input-edge correction

Final documentation-head CI `34323584335` is not green. Validate,
project-contract, compatibility, browser shards 2/3 and 3/3, and immutable
deployment smoke passed; browser shard 1/3 recorded **60 passed / 1 failed**.
The sole failure was the Firefly Lantern perfect Normal completion case, which
timed out waiting for `firefly-difficulty-selector` after the eight-cycle
journey.

Artifact download was denied by the local authorised proxy, but the reported
error context was reproduced locally without altering the 45-second test
budget. A short `keyboard.press('Enter')` could dispatch key down and key up
between consecutive Phaser updates. The named target then disappeared because
its lifetime expired rather than because it was caught; the result correctly
showed **7 / 8**, did not unlock replay modes, and could never expose the
requested difficulty selector. The same sampling race existed for the selector
keys.

Candidate `c7fd62f199c4dfd1bb01475dcc658cd1005997f0` keeps each real Enter, M and 1
key edge down until the corresponding named UI transition is observed, then
releases it. This retains all eight catches, the result/unlock journey, real
keyboard interaction, the Normal difficulty assertion and the unchanged
45-second budget. Three consecutive focused runs passed locally. Exact-head
three-shard, compatibility and Validate evidence is still required before
technical qualification can be restored. Runtime code and reviewed visual
evidence are unchanged; UV-D014 still requires David's explicit approval after
technical qualification.

### Firefly correction qualification

The first pushed correction documentation head `40d0357` produced a non-green
CI run `34326822414`; its shard 1 artifact remained unavailable through the
local authorised proxy, so it is not used as positive evidence. A clean
follow-up exact head `310dd9e` ran the unchanged correction through the complete
required matrix in CI `34328261135`: shard 1 passed **61**, shard 2 passed **58**
with **3 configured skips**, and shard 3 passed **60**, totalling **179 passed /
0 failed / 3 configured skips / 0 unrun = 182**. The corrected Firefly journey
passed in shard 1. Validate passed **114 files / 435 tests**, build/static smoke
and **480.01 KiB** under the unchanged **520 KiB** limit; compatibility passed
**48 / 15 configured skips**.

This correction changes only test input synchronisation and durable evidence.
Runtime candidate `febfe2db78cdd338662ecc05febf22f8aa70d890`, reviewed four-class
images and immutable preview `https://890a12f5.unicorn-valley.pages.dev` remain
unchanged. Exact-head Actions smoke `34328261022` passed isolated startup,
creator save, reload and Continue against that manifest-bound deployment;
artifact `10094683048` is named for the same runtime SHA.

Technical qualification is restored, but human acceptance is not. Under
UV-D014 David must review `docs/evidence/WP18K-VISUAL-REVIEW-INDEX.md`,
acknowledge the inherited phone Bag/Settings limitation assigned to WP19F,
confirm the **179 / 3** browser and **48 / 15** compatibility dispositions and
the immutable save/Continue smoke, then explicitly approve or reject WP18K.
No merge, production deployment, WP19B or R7 action is authorised before that
approval.


## David preview correction — real Map drag and modal chrome

David’s 9 September preview playtest is authoritative over the preceding green geometry evidence: the Map did not drag under his real mouse/touch gesture, so WP18K is not accepted and the earlier CI run is historical rather than proof of this behaviour. Investigation found a drag-ownership defect in `InventoryScene`: `bag-map-content` was created before the geography, but consolidation selected objects before that container in the display list. The draggable container was therefore empty. Gestures changed its coordinates while every route, node and label remained stationary; previous green tests asserted only the empty container coordinate and missed the visible failure. Rendering-only map cameras are now excluded from input ownership as an additional guard.

Runtime candidate `aa68c604c40eb34d1d25c8c5414ed1293b3a2255` places every route, node and label created after `bag-map-content` into that canonical draggable container, while retaining the real Phaser mouse/touch drag zone and clamping it to meaningful two-axis travel. The geography is deliberately composed beyond the clipped window rather than arbitrarily scaling an already fitting map. A geometry mask reinforces the existing map camera clip; North and the parchment frame remain outside the moving container. New focused coverage enters through the normal exploration HUD and asserts rendered landmark displacement relative to the unchanged frame/North under desktop mouse and tablet touch events.

The same candidate removes the whole coloured icon/badge from both Bag and Map, removes the Map subtitle/stars, and separates the heading, larger viewport, explanatory line and guidance vertically. Wonderbook design remains otherwise unchanged; its cross-only close moves right, uses warm pale gold against the purple cover and retains a 72 × 70 logical invisible target wholly inside the cover. Focused Map/Bag/Wonderbook qualification passed **5/5** locally with all interaction assertions retained. Fresh four-class evidence is indexed by `docs/evidence/WP18K-VISUAL-REVIEW-INDEX.md`. Full exact-head CI, immutable candidate publication and manifest-bound deployment smoke remain required before technical qualification can be claimed. UV-D014 remains unchanged.
