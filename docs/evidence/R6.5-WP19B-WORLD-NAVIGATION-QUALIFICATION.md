# R6.5-WP19B world-navigation qualification

Evidence date: 9 September 2026. Branch: `agent/r6.5-wp19b-world-boundaries-navigation`. Draft PR: #162.

## WP18K release verification

PR #161 merged as main `250f0855f013f4ba54a6de4056181cf5affa0d57` after David's explicit visual approval and exact-head qualification. Cloudflare's check on that exact merge SHA reports successful deployment `https://7a667b82.unicorn-valley.pages.dev` (deployment ID `7a667b82-7d62-4c5b-b5fd-9720557a6306`). The production alias `https://unicorn-valley.pages.dev` then passed isolated startup, creator save, reload and Continue in Actions run `34345075418`, job `102444639698`, with the workflow checking out merge SHA `250f0855`. The local runner's CONNECT 403 was not bypassed.

The first PR-triggered smoke `34345012257` stopped before browser execution because the URL validator accepted immutable subdomains but not the production alias itself. The bounded workflow correction now permits only the exact production hostname or an immutable `*.unicorn-valley.pages.dev` hostname; no broader host is accepted.

## Cottage floor-boundary correction

The displayed skirting/floor seam is world `y=390`. Previously `wall-top` ended at `y=140`, allowing the player's feet/body to enter the illustrated back wall and window region. The blocker now spans the back wall through `y=390`; the seam is a named foreground occlusion edge. At wall contact near `x=705`, the 112 × 92 complete unicorn is visible across `y=354..446`; its scaled Arcade body begins at the seam (`y=390`) and its feet remain on the floor at `y=410`. Current exact centre, visual bounds, body and feet values for every display are in `wp19b-current-head/geometry.json`.

The window nook and left/right wall decoration art remain at their established positions, but their interaction approaches are deliberately on the floor at `y=455/445/445`. The treasure shelf approach moved to `(1310,470)`. Willow and Nova visits moved from wall-overlapping `y=430` to `y=470`. Exit, centre/floor slots and tea-table approach remain reachable. Empty and occupied decoration presentation is unchanged.

## Tap navigation and movement classification

`CrystalGrottoScene` and `FireflyGroveScene` now participate in the canonical click/tap manager. Each has an authored traversal map matching its physics bounds and named crystal/tree/plant/pool colliders. A blocked tap resolves to the nearest safe cell; the next open tap replaces the target, demonstrating stuck-target recovery. The shared path finder already rejects diagonal corner cutting. Keyboard/pad motion, Gallop multiplier, facing selection and race movement are unchanged.

Movement-supported exploration scenes:

- Moonflower Glade, Sunbeam Village, Rainbow Meadow, Crystal Brook, Whispering Woods and Starlight Beach;
- Moonflower Cottage, Moonflower Patch, Hollow Tree Nook and Windmill Lookout;
- Crystal Grotto and Firefly Grove.

Intentionally static or separately controlled screens:

- Title, creator, Bag/Map, Wonderbook, Settings, Cottage Decorate, shops and village interiors;
- story/dialogue scenes, picnic, egg hatch and doorway/entry screens;
- Nova tutorial race, race, and Firefly Lantern, which retain their own activity controls.

Boot, preload and diagnostic-only scenes are infrastructure rather than explorable player screens.

## New-game location agreement and compatibility

New saves now record `location:moonflower-glade`, matching the creator's actual first scene, Map's **You are here** node, checkpoint and Continue destination. The legacy `moonflower-cottage` ID is retained and still resumes existing saves into Moonflower Cottage. `location:moonflower-cottage-interior` and every other established resumable ID remain unchanged. This is a default-value correction, not a schema migration.

## Visual evidence

`docs/evidence/wp19b-current-head/` contains four whole-unicorn Cottage wall views:

- `desktop-cottage-wall.png`
- `tablet-landscape-cottage-wall.png`
- `phone-landscape-cottage-wall.png`
- `phone-portrait-cottage-wall.png`

It also contains `tablet-landscape-grotto.png`, `tablet-landscape-grove.png` and `geometry.json`. The Cottage images show the complete unicorn at the window approach, not only its collision body. The accepted HUD/Bag/Map/Book presentation is preserved.

## Technical results

- Release smoke: Actions `34345075418`, **passed** against the production alias with candidate `250f0855`.
- Focused Vitest traversal/save group: **20 passed / 0 failed**.
- Focused Playwright movement group: **10 passed / 0 failed**, covering Cottage keyboard/Gallop/tap, backwards facing, Grotto/Grove touch and the existing Cottage/Grotto/Grove depth journeys.
- `npm run validate`: **116 files / 441 tests**, build/static smoke and entry **481.00 KiB** under the unchanged **520 KiB** limit.
- Browser compatibility and complete sharded CI: pending exact-head evidence.

Configured skips remain distinct from passes. No test, cycle, assertion or timeout was weakened.

## David collision/movement sign-off checklist

- [ ] The complete unicorn stops on the floor side of the Cottage back-wall/window seam.
- [ ] Window, shelf, occupied/empty decoration slots and exit remain comfortably approachable.
- [ ] Keyboard diagonal movement, wall rubbing, Gallop and repeated Cottage entry/exit feel normal.
- [ ] Desktop click and tablet/phone tap route around Cottage furniture; blocked taps recover.
- [ ] Crystal Grotto and Firefly Grove taps move around visible obstacles and retain their actions/exits.
- [ ] Backward tap movement keeps a stable facing direction.
- [ ] A new game opens in Moonflower Glade and Map/Continue identify Moonflower Glade.

WP19B must not merge or deploy, and WP19C must not start, until David explicitly approves this gate under UV-D014.

## Immutable WP19B candidate

Cloudflare reports exact documentation/evidence head
`69746bb3008fd9824704badc9e24e3bc55147712`, containing runtime `0f980b6`, at
`https://5b6e9886.unicorn-valley.pages.dev`. The manifest now binds the next
Actions smoke to that URL and exact deployed SHA. No candidate smoke is claimed
until the resulting workflow completes.
